# Slack API Tokens
# Get these from https://api.slack.com/apps
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_APP_TOKEN=xapp-your-app-token-here

# LLM Provider API Keys
# Choose one or more providers based on your configuration

# Anthropic API Key (required if using provider: anthropic)
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# OpenAI API Key (required if using provider: openai)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# AWS Configuration (required if using provider: bedrock)
# Make sure your AWS credentials are configured
# Either through ~/.aws/credentials or environment variables
AWS_REGION=us-east-2
# AWS_ACCESS_KEY_ID=your-access-key (optional, if not using default credentials)
# AWS_SECRET_ACCESS_KEY=your-secret-key (optional, if not using default credentials)

# Tool API Keys
# OpenWeatherMap API Key (required if using openweathermap tool)
# Get from: https://openweathermap.org/api
OPENWEATHERMAP_API_KEY=your-api-key-here

# Application Configuration
CONFIG_PATH=config/config.yaml
